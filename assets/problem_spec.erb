# frozen_string_literal: true

require 'leetcode_ruby'

RSpec.describe LeetcodeRuby::<%= module_name %> do
  let(:instance) { Object.new.extend LeetcodeRuby::<%= module_name %> }
  it '' do
    expect(instance).to eq()
  end
end
