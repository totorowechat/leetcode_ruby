# frozen_string_literal: true

require 'leetcode_ruby'

RSpec.describe LeetcodeRuby::<%= module_name %> do
  describe '' do
    it '' do
      input = ''
      instance = LeetcodeRuby::<%= module_name%>::Solution.new(input)
      expect(instance.run()).to eq()
    end
  end
end
